[[stack]]
name = "acme-dns"
description = "Serveur DNS pour challenge acme"
deploy = true
tags = ["edge"]
[stack.config]
server = "Local"
git_provider = "github.com"
repo = "PortableStick/homelab-infra"
file_paths = ["hosts/vps-prod/stacks/acme-dns/compose.yaml"]